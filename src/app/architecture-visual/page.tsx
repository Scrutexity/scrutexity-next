import { ArchitectureVisualWidget } from '@/components/ArchitectureVisualWidget';

export const metadata = {
  title: 'Architecture Visual | Scrutexity',
  description: 'The path from leaked inquiry to verified booking.',
};

export default function ArchitectureVisualPage() {
  return <ArchitectureVisualWidget />;
}
